The two truly broken units (17 and 24) both have pooled R² around −1.0 and individual R² of −10.4 and −1.7. Looking at their stats — unit 17 has rt_cv=0.99, unit 24 has rt_cv=1.01, both with ~249 train days. Normal variance, normal data volume. These aren't data quality issues. Something about their behavioral pattern is genuinely discontinuous — possibly a thermostat that was replaced mid-dataset, a schedule change, or erratic occupancy. Worth spot-checking their raw time series.
The UNDEFINED units (10, 23, 25) have 296 train days but 0 test rows — they fall entirely before the cutoff. Unit 18 has only 138 train days and 0 test rows. These are fine, just timing artifacts.
The pooled vs individual R² divergence is interesting. Unit 75: pooled R²=0.855 but individual=−0.120. That means the pooled model learned cross-thermostat patterns that transfer well to unit 75, but a model trained only on unit 75's own history is useless. Unit 75 is probably a "typical" thermostat that the global signal captures perfectly, but whose own data is too noisy or sparse to self-model.
What would actually move the needle from here:
The MODERATE cluster (36 units, mean 0.378) is the real opportunity — these have adequate data and variance but the model isn't capturing their patterns. Rolling features (7-day runtime mean/std, runtime streak length) would likely help most here since lag_1 alone can't capture momentum well for units with irregular on/off cycles.